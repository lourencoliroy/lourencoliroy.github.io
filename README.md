# lourencoliroy.github.io

Personal site for Liroy Lourenço — a single static `index.html` (no build step).

Served by GitHub Pages from the `main` branch. Edit `index.html` and push;
`.nojekyll` tells Pages to serve the files as-is without Jekyll processing.
